<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heyux1n.shopping.mall.manager.mapper.OrderStatisticsMapper">

    <insert id="insert">
        insert into order_statistics (order_date, total_amount, total_num)
        values (#{order_date},
                #{totalAmount},
                #{totalNum});
    </insert>


    <resultMap id="OrderStatisticsMap" type="com.heyux1n.shopping.mall.model.entity.product.OrderStatistics"
               autoMapping="true"/>

    <sql id="columns">id, province_code, order_date, total_amount, total_num, create_time, update_time</sql>

    <select id="selectListByTime" resultMap="OrderStatisticsMap">
        select
        <include refid="columns"/>
        from order_statistics
        <where>
            <if test="createTimeBegin != null and createTimeBegin != ''">and order_date>=#{createTimeBegin}</if>
            <if test="createTimeEnd != null and createTimeEnd != ''">and order_date &lt;=#{createTimeEnd}</if>
        </where>
    </select>
</mapper>